// Generated by CoffeeScript 1.3.3
(function() {
  var connect;

  connect = function() {
    var c, host;
    host = document.getElementById('host').value;
    c = new irc.IRC(host, 6667);
    c.connect();
    return;
    return chrome.experimental.socket.create('tcp', '127.0.0.1', 8080, function(socketInfo) {
      var id;
      id = socketInfo.socketId;
      console.log(id);
      return chrome.experimental.socket.connect(id, function(result) {
        console.log(result);
        return chrome.experimental.socket.write(id, "Hello, world!", function(sendInfo) {
          return console.log(sendInfo);
        });
      });
    });
  };

  document.getElementById('connect').onclick = connect;

}).call(this);
